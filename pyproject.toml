[project]
name = "heylen-contract-generator"
version = "0.1.0"
description = "Interne contractgenerator voor Heylen Vastgoed - tweezijdige aankoopbelofte"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
